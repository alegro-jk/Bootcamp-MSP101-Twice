namespace SongsProfile
{
    class  KnockKnock : SongsProfile
    {
        public KnockKnock(string songTitle, string language, int yearReleased, string album)
        {
            SongTitle = songTitle;
            Language = language;
            YearReleased = yearReleased;
            Album = album;
        }
    }
}