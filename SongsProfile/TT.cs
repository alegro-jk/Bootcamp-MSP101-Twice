namespace SongsProfile
{
    class  TT : SongsProfile
    {
        public TT(string songTitle, string language, int yearReleased, string album)
        {
            SongTitle = songTitle;
            Language = language;
            YearReleased = yearReleased;
            Album = album;
        }
    }
}