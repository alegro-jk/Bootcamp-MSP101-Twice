namespace SongsProfile
{
    class  TheFeels : SongsProfile
    {
        public TheFeels(string songTitle, string language, int yearReleased, string album)
        {
            SongTitle = songTitle;
            Language = language;
            YearReleased = yearReleased;
            Album = album;
        }
    }
}