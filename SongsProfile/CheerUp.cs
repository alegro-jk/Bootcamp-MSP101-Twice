namespace SongsProfile
{
    class  CheerUp : SongsProfile
    {
        public CheerUp(string songTitle, string language, int yearReleased, string album)
        {
            SongTitle = songTitle;
            Language = language;
            YearReleased = yearReleased;
            Album = album;
        }
    }
}